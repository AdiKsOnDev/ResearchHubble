function ChatBox() {
  const [myMessages, setmyMessages] = useState([]);
  const [llmMessages, setllmMessages] = useState([]);

  return ( 
    <main className="w-max bg-metal h-max">
      
    </main>
  );
}

export default ChatBox;